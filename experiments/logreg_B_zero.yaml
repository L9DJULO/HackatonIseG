# Bloc B seul, bord à zéro (comparaison avec la convolution normalisée).
run_name: logreg_B_zero
seed: 0
features:
  - name: gaussian
    border: zero
model:
  name: logreg
  config: {C: 1.0, max_iter: 300}
sampling:
  n_per_class: 5000
  boundary_frac: 0.0
  prior_correction: true
