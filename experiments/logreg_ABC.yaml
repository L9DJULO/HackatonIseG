# Référence de l'ablation du bloc D : A + B + C, sans morphologie.
run_name: logreg_ABC
seed: 0
features:
  - name: intensity
  - name: gaussian
  - name: spatial
model:
  name: logreg
  config: {C: 1.0, max_iter: 300}
sampling:
  n_per_class: 5000
  boundary_frac: 0.0
  prior_correction: true
