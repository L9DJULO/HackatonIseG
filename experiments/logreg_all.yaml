# Ablation : A + B + C + D (tous les blocs).
run_name: logreg_all
seed: 0
features:
  - name: intensity
  - name: gaussian
  - name: spatial
  - name: morpho
model:
  name: logreg
  config: {C: 1.0, max_iter: 300}
sampling:
  n_per_class: 20000
  boundary_frac: 0.0
  prior_correction: false
